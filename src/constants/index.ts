import images from './images';
import {
  COLORS,
  OS,
  SPACING,
  FONTSIZE,
  BORDER_RADIUS,
  FONT_FAMILY,
} from './theme';

export {COLORS, images, OS, SPACING, FONTSIZE, BORDER_RADIUS, FONT_FAMILY};
